# Investment Research Copilot — Python dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# Tested with Python 3.11+

# ── Core inference stack ──────────────────────────────────────────
torch>=2.0.0
transformers>=4.40.0
peft>=0.10.0
accelerate>=0.27.0

# ── Market data ───────────────────────────────────────────────────
yfinance>=0.2.30
pandas>=1.5.0

# ── Training scripts (optional — only needed to retrain) ──────────
PyYAML>=6.0

# ── Testing ───────────────────────────────────────────────────────
pytest>=7.0

# ── Notes ─────────────────────────────────────────────────────────
# No GPU required. Model runs on CPU/MPS (Apple Silicon) via stub fallback.
# yfinance requires network access; the pipeline falls back to a stub
# automatically if yfinance is unavailable or the network is offline.
